bld.new_task_gen(
    features = 'cc cprogram',
    packages = 'config gio-2.0 gtk+-2.0 posix',
    vapi_dirs = '.',
    source = 'Mound.vala UI.vala',
    target = 'mound',
    uselib = 'GTK',
    ccflags = '-g',
)

bld.new_task_gen(
    features = 'subst',
    source = 'mound.ui.in',
    target = 'mound.ui',
    dict = {'VERSION': bld.env['VERSION']},
    install_path = '${PREFIX}/share/mound'
)

